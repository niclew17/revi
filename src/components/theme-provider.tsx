"use client";

import * as React from "react";

// Placeholder theme provider - next-themes not installed
export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
